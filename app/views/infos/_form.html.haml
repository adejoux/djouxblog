= simple_form_for(@info) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :permalink
    = f.input :content, :input_html => {:size => 80, :rows =>5}

  .form-actions
    = f.button :submit
