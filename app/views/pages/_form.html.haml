= simple_form_for(@page) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :category, collection: ['posts', 'wiki']
    = f.input :tag_list
    = f.input :summary
    = f.input :content
    = f.input :published

  .form-actions
    = f.button :submit
