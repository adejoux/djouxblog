%h1{ :id => "page_title"}= page.title
= render :partial => 'pages/subtitle', :locals => { :page => page }

.lead{ :id => "page_summary"}= raw page.summary
%div{ :id => "page_content"}= preserve page.rendered_body
